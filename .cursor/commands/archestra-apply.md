---
name: "/archestra-apply"
id: "archestra-apply"
category: "Workflow"
description: "Plan, build, test, demo-record, then verify each frame against the OpenSpec"
---

Run the `archestra-apply` skill: plan → build → test → demo-record → verify-per-frames-and-spec.

Read `.cursor/skills/archestra-apply/SKILL.md` and follow every phase in order. Default change: the active OpenSpec change, or `local-first-font-editor` if it is the only one.
